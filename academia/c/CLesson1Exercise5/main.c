#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    printf("This is a program with a");
    printf("problem!"); /* This do_it(); doesn't exist in the C library but printf() does the exact same thing but more friendly ;-) */
    return 0;
}
