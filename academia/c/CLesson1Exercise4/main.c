#include <stdio.h>
#include <stdlib.h>

int main(void) //;found it, semicolon can't be here because it would defeat the purpose of running the program in the first place.
{
    printf("Keep looking!\n");
    printf("You\'ll find it\n");
    return 0;
}
